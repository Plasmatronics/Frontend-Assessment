import AppLayout from "../components/AppLayout";

export default function ChatPage() {
  //even if we expanded this app an extraordinary amount, no logic would ever go into this page
  return <AppLayout />;
}
